# tribe.html — 部落首领 — 项目进度

## 部署上线 + 手机适配 + 每天自动同步（2026-08-04）
用户要求跟另一款游戏（卡牌地牢/CD0.1文件夹）一样，有一个随时能访问的公网链接、每天自动同步、界面能在手机上用。照着 CD0.1 那套已经在用的模式原样复刻了一份：
- **公网链接**：https://hua26111222.github.io/Tribe-Chieftain/ ——用 GitHub Pages 托管，仓库是公开的 `hua26111222/Tribe-Chieftain`（免费版 Pages 必须公开仓库，已经跟用户确认过，内容不涉密）。
- **手机适配**：tribe.html 补了 viewport 标签（原来完全没有，手机打开会整个缩得很小）、加了窄屏（≤760px）的响应式样式——原来的左右两栏经营页在手机上会自动改成上下堆叠，花名册表格能横向滑动，按钮和字号也做了适配。SPEC.md §0 的"最小宽度1200px"硬约束跟着改了，允许手机窄屏。
- **每天自动同步**：Windows 任务计划里建了一个「Tribe-Chieftain Auto Sync」任务，**每天晚上9点**自动把这个文件夹的改动 commit 并推送到 GitHub，Pages 上的版本几分钟内就会跟着更新。跟用户核对过：CD0.1 那个旧任务实际是"每5分钟"跑一次（不是"每天"），而且现在是关闭状态；这次用户明确选了"每天一次"，所以部落首领用的是每天，不是5分钟。
- **踩过的坑，记一笔**：一开始直接把带中文的文件夹路径（"部落首领"）写死在 auto_sync.ps1 和任务计划的参数里，Windows PowerShell 读取中文路径字符串时会读错编码，导致脚本内部定位不到项目文件夹。改成用 `$PSScriptRoot`（脚本自己所在的文件夹，运行时自动识别，不用再手写中文路径）彻底避开了这个问题——用 `Start-ScheduledTask` 真实触发过一次，确认能正确 commit+push 到 GitHub 才算数，不是只看着任务"已注册"就当完事。
- CD0.1 和 Hunkui 文件夹里还留着两份旧版 `chieftain.html`（部落首领拆分出独立文件夹之前的旧快照），当时没同步清理，如果用户想要可以帮忙删掉，这次没动它们。

> 需求来源是本文件夹里的 `SPEC.md`（24人部落经营+守城战），产物是本文件夹里的 `tribe.html`，双击即可玩。
> 这个项目原来和用户其他游戏混在同一个文件夹（CD0.1），2026-08-04 已拆分成独立文件夹单独存放。

## v1.1：换头、远征/明矿、建设、地图化界面、花名册折叠（2026-08-04）
用户实际试玩第一期后提出四条新需求：①探险队找资源/找人/找明矿（明矿要"是个大事"）；②界面要更图形化，类似英雄无敌3那种有画面的经营页；③花名册太占地方要能收起来；④"任免"要能真的把某个头换成另一个人当。

**先处理了和 SPEC.md 的直接冲突**：探矿/寻矿寻人在 §17"明确不做"里被列为不做，图片/动画在 §0 被列为视觉禁止，跟用户最初"唯一需求来源，不要自行发挥"的指示矛盾。跟用户确认后，选择"先改 SPEC.md 再照着做"，而不是绕开 SPEC 偷偷加功能。改动前用 EnterPlanMode 走了一遍完整方案，用户批准后才动手。

**改完 SPEC.md 的内容**：
- §0 视觉例外：允许手写 SVG/纯 CSS 画的示意图形表示地图和建筑，仍禁止位图/渐变/阴影/圆角/动画。
- §7.1 追加"换头"细则：任何一个头随时能换人，条件是新人选活着、mind 档位"甲"（≥6）、当前不是别的头，花1行动点，威望−3。
- 新增 §11.x 远征系统：三个写死目的地（附近山谷/废弃矿坑明矿/邻村），**全案零随机（§0铁律），远征没有"概率"只有"门槛"**——达不到门槛按钮直接禁用，不是"派了但失败"。明矿门槛≥4人且mind总和≥12，占2轮，一次性铁+60石+30并解锁"驻矿"岗位；邻村门槛≥2人mind总和≥10，带回写死人物"霖"（24人变25人，用户确认接受这会削弱"人手永远不够"的原始张力）。
- 新增 §7.4 建设系统：粮仓/药庐/工坊扩建/哨塔四种建筑，复用"工匠每人每轮一选"机制，跟修墙同门槛。
- §17"不做"列表加了修订说明：迁移/探矿地图、寻矿寻人不是无限制开放，只是这三个写死目的地。

**tribe.html 对应实现，自查时特别注意了不重蹈上次两个bug的覆辙**（st.log/turnLog混用、副作用挂在每次render都会跑的函数里）：
- `state.heads` 从写死4个id改成可变映射，`POST_OWNER` 也跟着改成按头衔（而不是id）动态查，这样换头后超编惩罚计算才会用新头的数据。
- 远征跨轮结算：`resolveExpedition` 放在 `resolveProduction` 之后调用，人力在 `isAvailable()` 里用 `state.expedition.active` 判断是否在外，回来时才让资源/人真正到账，中途状态不会提前生效或被过早清空。
- 花名册折叠用 `<details>`；**踩坑点记录**：因为 `render()` 每次都整个重建 DOM，`<details>` 的展开状态如果不额外存一个模块级变量（`rosterOpen`）就会在用户每点一下别的控件时被强制收回去——已经用一个跨 render 保留的变量修好，没有直接踩进去，但值得记一笔，以后加别的"折叠/展开"UI要留意同样的坑。
- 地图面板 `renderMap()` 用手写 SVG 画6段城墙+4种建筑+明矿的极简色块图标，点击跳转到对应操作面板，不新建一套平行数据/交互逻辑，只是现有 state 的"另一种画法"。

**本期做了但记录在案的判断/简化，供用户指正**：
- 远征三个目的地对"出远门"的昏度代价，SPEC 原文只在"附近山谷"写了 fog+6，明矿/邻村没写，实现时统一按 fog+6 处理（三处都是出远门，理由一致）。
- 明矿/邻村是全局唯一一队在外（不支持同时派多队出征），派人时如果已有一队在外，按钮直接禁用，界面会写清楚原因。
- 建筑建成后，如果玩家不主动把对应工匠的手艺任务切换掉，"已经建好了"这条提示会重复出现（不会重复扣料，纯粹是提示重复）——已经在建成的那一刻把 `p.task` 自动重置回"工匠"来避免这个情况。
- 本机仍然没有 Node/浏览器自动化，这次改动同样是逐行通读代码把关，**强烈建议用户实际试玩换头、远征（尤其是明矿这种跨2轮结算的）、建设这几块新功能**，把界面死角反馈回来。

## 第一期完整版：9轮经营（无战斗）可双击试玩，自查修了4处真实bug（2026-08-04）
用户确认第一步没问题后，让继续做完第一期剩下的部分。按 SPEC.md §5 §6 §7 §11 §14.1 §14.2 把完整的经营循环和界面搭了起来，`tribe.html` 现在双击能打开、能一轮一轮玩到第9轮：
- **每轮流程**：早报（四头按档位措辞）→ 危机事件（§11 九条写死事件，含选项）→ 排人上岗+工匠选任务（造箭/修墙/修装备/带徒）→ 内圈调整（每次改动花1行动点）→ 结束本轮结算（产出→秋收/春播→取暖→吃粮→昏度）
- **写完自己又通读了一遍代码，揪出并修了4处真实bug，都不是用户发现的，是我自己复查时找到的**：
  1. 初始排岗时，会因为"身体不够格"把规格书自己给的初始岗位挤掉（比如年幼的孩子被从"轻活"强制挤到"闲"）——现在改成起始岗位永远保留可选，门槛只管新指派
  2. "造箭"这个手艺任务写错了变量名（`st.ammo箭`），而且起始状态里根本没建`ammo`这个数据结构，实际运行会直接报错崩溃——已修好并补上了§5要求的箭/枪/石弹起始数量
  3. 超编惩罚的"该头名下岗位"算法，一开始把"闲""轻活"这些不产出任何东西的岗位也算进头的管理人数里，导致游戏一开局（谁都还没动）就已经在无意义地扣产出——改成只有真正产出的岗位才占用头的名额
  4. "修墙"这个手艺任务不管选哪里，永远只会修第1段城墙，玩家没法指定修哪一段——现在改成6段城墙各自可选，"亲自查看城墙"这个行动也补上了选段下拉框（原来也是写死只能看第1段）
- **规格书里有需要我自己拿主意的模糊地带，如实告诉用户，供指正**：
  - §7.2"超编惩罚：某岗位人数超过该头capacity"没写清楚哪个岗位归哪个头管，自定了一套划分：守备→军头，下地/打猎→政头，砍木/采石/挖废墟/工匠→工匠头，谍头暂不管人
  - §6 的 `GRAIN_DECAY`（存粮季节性损耗）定义了但正文没说在哪用，按"存粮受潮"的合理场景接上了，每轮吃粮前先扣一道
  - 事件2选项C"3人病倒"、事件1选项A"派4人挖沟"具体选谁，规格书没写，按"优先选不在内圈、身体弱的人"这个思路自己定的
  - 用 Python 手工核对了"不砍木会冻死人"的公式：按§5起始木材20、§6需柴表精确计算，柴恰好在第4轮结束时耗尽为0（不多不少），第5轮才会真正出现挨冻——跟 SPEC.md §15 验收文字写的"第4轮起"差了一轮。这是formula本身算出来的精确结果，我按公式实现，没有为了凑"第4轮"这个数字去改公式，如果你希望强制在第4轮就出问题，需要调整初始木材或需柴数值，告诉我
- **本期明确没做的**（属于第二/三/四期范围，不是漏做）：守城战斗本身（第3/6/9轮目前只弹一行提示说"这里本该开打"，跳过战斗判定）、装备穿戴、编队、谍头情报质量、工匠头贪污虚报、手艺等级细节、家族视图、私聊消息
- 本机没有 Node.js/浏览器自动化环境，没法用工具真的点开跑一遍；这一步的正确性靠"从头到尾通读一遍源码逻辑+手工复算取暖/农事公式"来把关，**强烈建议用户自己双击 tribe.html 实际玩一整局**，把界面显示、点击是否顺手、有没有我没想到的死角这些反馈给我

## 第一期·第一步：数据表 + 四个核心函数，逐行核对通过（2026-08-04）
用户提供了 SPEC.md（发现用户最初在对话里贴的版本中文全部乱码、表格里的✓✗对错也分不清，让用户重新给了文件路径，读取后确认内容完整无损）。
- 按 SPEC.md §18 的第一步要求：只建 §4.2 的 24 人 `PEOPLE` 数据表和 §2 的四个核心函数（`mind` `body` `tier` `capacity`，另加 §3+§9.1 组合出的"能否当队长"判定），写进了 `tribe.html`，还没有做任何界面
- **验收方式**：本机没有 Node.js，`console.table`/`console.log` 没法直接跑；用 Python 严格照抄了同一套公式（含 JS 的 `Math.round` 四舍五入规则）在环境里单独跑了一遍，逐行核对了 24 人的 body/mind/档位/能否上墙/能否出圈/能否当队长，与 SPEC.md §4.3 的期望表**完全一致**（0处不同），四项关键统计也全部吻合：能上墙13人、能出圈11人、能当队长5人（霜/河/锤/钉/七）、能当头5人（霜/二/河/锤/七）
- 按 SPEC.md §18 原文"第一步做完先给我看结果，别继续"的明确要求，这一步验证通过后先停下来，没有自动往下做经营循环/界面等后续内容
- 下一步：等用户确认这一步没问题后，继续做第一期剩余部分（§5 起始状态、§6 季节时间、§7 一轮流程、§11 危机事件、§14.1/§14.2 界面），目标是"9轮经营，能看24人活不活得过两个冬天"
